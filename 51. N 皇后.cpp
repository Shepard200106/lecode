#include"headFile.h"
class Solution {
public:
    vector<vector<string>> solveNQueens(int n) {
        auto solutions = vector<vector<string>>();
        auto queens = vector<int>(n, -1);
        auto column = vector<int>();
        auto diagonals1 = vector<int>();
        auto diagonals2 = vector<int>();
        return solutions;
    }
    void backtrack(vector<vector<string>>& solutions, vector<int>& queens, vector<int>& coulumn, vector<int>& diagonals1, vector<int>& diagonals2, int n, int row) {

    }
};